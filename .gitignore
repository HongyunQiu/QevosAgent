# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
.env

# Local experiment outputs / snapshots (runtime data, not source)
agent_memory.json
agent_snapshot.json
agent_snapshot_meta.json
agent_tools.json
memory_episodic.jsonl
memory_concept.md
agent-reach/
~/.agent-reach/
~/.agent-reach-venv/
.claude
.cursor
.codex

# Local dirs / generated artifacts
.worktrees/
artifacts/
fff.nvim/
*.vcd
fdtd_output*/

# Logs / pids
*.log
*.out
*.pid

# Runs directory (raw memories, scratchpads)
runs/
raw_memory.ndjson

# UI App (runtime:web) per-app project data — runtime state, not source
app-data/
# UI App 源码工程工作区（含 node_modules；只 ship 构建产物 dist/，源码不入库）
app-src/
# UI App 构建产物（工程型 App 的 dist/；本地/运行时产物，不入库）
apps-dist/

# Misc
.DS_Store

# Memory files
memory_macro.md

# python environment
venv/

# others
config/
.code-review-graph/

SKILLS/*
CLAUDE.md

# Cron runtime state (per-machine, not source)
crons/.history.jsonl
crons/.pending.json

# Watcher runtime state (per-machine, absolute paths, not source)
.qevos/

# Cron 生成的报告（每机产物，不入库）
reports/

# Local release scripts (private build tooling)
release.ps1
release.sh
mobile/install.ps1
